class Test
{
   public static void main(String...aeg)
   {
     int a=5,b=6,c=1;
     int r=(b*b)-4*a*c;
    double R= Math.sqrt(r);
    double A=((-b)+R)/2*a;
    double  B=((-b)-R)/2*a;
      System.out.println(A+" "+B);
   }
}  